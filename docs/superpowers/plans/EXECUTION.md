# Execution mode

This implementation is being executed inline on an isolated feature branch because this environment does not expose subagent execution. Each feature follows test-first RED → GREEN verification through GitHub Actions before moving to the next task.

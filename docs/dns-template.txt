# QuanticMail DNS starter template for quanticsillage.com
# Replace SERVER_IPV4 and generated values before publishing.
# Stalwart's generated zone file is authoritative for DKIM selectors and final policies.

# Mail host
A     mail                     SERVER_IPV4

# Incoming mail
MX    @                        10 mail.quanticsillage.com.

# Sender Policy Framework
TXT   @                        "v=spf1 mx -all"

# DMARC — start in monitoring mode, then tighten after validation
TXT   _dmarc                   "v=DMARC1; p=none; rua=mailto:dmarc@quanticsillage.com; adkim=s; aspf=s"

# DKIM
# DO NOT invent this value. Copy the selector(s) and public key generated by Stalwart.
# TXT   <selector>._domainkey  "v=DKIM1; ..."

# Recommended discovery/service records
# Copy the exact SRV/autoconfig/autodiscover/MTA-STS/TLS-RPT records generated by Stalwart.

# Reverse DNS / PTR is configured at the VPS provider, not in the public DNS zone:
# SERVER_IPV4 -> mail.quanticsillage.com
